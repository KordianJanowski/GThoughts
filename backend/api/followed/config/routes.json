{
  "routes": [
    {
      "method": "GET",
      "path": "/followeds",
      "handler": "followed.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/followeds/count",
      "handler": "followed.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/followeds/:id",
      "handler": "followed.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/followeds",
      "handler": "followed.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/followeds/:id",
      "handler": "followed.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/followeds/:id",
      "handler": "followed.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
